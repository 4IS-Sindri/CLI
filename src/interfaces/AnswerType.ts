/**
 * The type of object that the resulting answer will be. Can be any of the
 * following:
 *
 * - `string`: A single-line string answer.
 * - `boolean`: A boolean answer.
 * - `number`: A number answer.
 * - `array`: An array of any of the above.
 */
export type AnswerType = 'string' | 'boolean' | 'number' | 'array'
